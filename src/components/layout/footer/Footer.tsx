import classes from "./style.module.css";

const Footer = () => {
    return (
        <footer className={classes["Footer"]}>
            <h2>Footer</h2>
        </footer>
    );
};

export default Footer;